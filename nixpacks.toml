# Configuração Nixpacks para Railway
# Este arquivo força o Railway a detectar este como projeto Python

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "chmod +x build.sh || true",
  "chmod +x start.sh || true",
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "echo '🔍 Detectando estrutura do projeto...'",
  "echo '📁 Diretórios encontrados:'",
  "ls -la | head -20",
  "echo ''",
  "echo '📁 Conteúdo de backend/:'",
  "ls -la backend/ || echo 'Diretório backend não encontrado'",
  "echo ''",
  "echo '✅ Build completo - Python Flask App'"
]

[start]
cmd = "bash start.sh"

